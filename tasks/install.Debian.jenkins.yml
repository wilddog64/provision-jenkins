---
- name: install jenkins
  apt:
    pkg: "{{ item }}"
    state: installed
  with_items:
    - jenkins
    - git
    - gcc-4.6
    - ruby-dev
    - make
    - autoconf
    - automake
    - rubygems
